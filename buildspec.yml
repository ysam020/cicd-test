version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.x
    commands:
      - cd client
      - npm install --legacy-peer-deps

  build:
    commands:
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: client/build
